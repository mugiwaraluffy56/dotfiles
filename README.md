# dotfiles

personal dotfiles managed with gnu stow.

```bash
git clone https://github.com/mugiwaraluffy56/dotfiles ~/dotfiles
cd ~/dotfiles
./install.sh
```

copy `.secrets.example` to `~/.secrets` and fill in your api keys. the shell loads it on startup.
